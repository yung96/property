# 🚀 Быстрый старт

## Запуск проекта за 3 минуты

### 1. Установка зависимостей

```bash
npm install
```

### 2. Запуск dev-сервера

```bash
npm run dev
```

Откройте браузер: http://localhost:5173

### 3. Структура приложения

```
📱 Выбор города (Сочи/Крым)
    ↓
📋 Категории недвижимости (10 категорий)
    ↓
🏠 Список объектов
    ↓
📄 Детальная страница объекта
```

## 🎨 Что уже реализовано

✅ **Дизайн**
- Темная тема с золотыми акцентами
- Адаптивность под мобайл и десктоп
- Плавные анимации

✅ **Функционал**
- Навигация между страницами
- Карточки городов, категорий, объектов
- Детальные страницы объектов
- Тактильная отдача (Haptic Feedback)

✅ **Интеграция**
- Telegram Web App SDK
- React Router для навигации
- Framer Motion для анимаций

## 📝 Что нужно изменить для продакшена

### 1. Замените моковые данные

Файл: `src/shared/constants/mockData.js`

Замените `MOCK_PROPERTIES` на данные из вашего API:

```javascript
// Было
import { MOCK_PROPERTIES } from '../constants/mockData';

// Стало
import { useQuery } from 'react-query';
const { data: properties } = useQuery('properties', fetchProperties);
```

### 2. Обновите изображения

Замените URL изображений в `mockData.js` на ваши реальные фото:

```javascript
image: 'https://your-cdn.com/property-1.jpg'
```

### 3. Настройте контакты

Файл: `src/pages/PropertyDetailPage.jsx`

Обновите телефон и ссылки:

```javascript
<a href="tel:+79991234567">  // Ваш телефон
<a href="https://wa.me/79991234567">  // WhatsApp
<a href="https://t.me/username">  // Telegram
```

### 4. Добавьте аналитику (опционально)

```bash
npm install @vercel/analytics
```

В `src/main.jsx`:
```javascript
import { Analytics } from '@vercel/analytics/react';
```

## 🎯 Проверка работы

### Локально в браузере

1. Запустите `npm run dev`
2. Откройте http://localhost:5173
3. Проверьте все страницы

### В Telegram (через ngrok)

1. Установите ngrok: https://ngrok.com/download
2. Запустите: `ngrok http 5173`
3. Скопируйте HTTPS URL
4. Создайте Mini App в @BotFather
5. Откройте через бот в Telegram

## 🐛 Возможные проблемы

### Ошибка: "Cannot find module"

```bash
rm -rf node_modules
rm package-lock.json
npm install
```

### Tailwind CSS не работает

```bash
npm run build
npm run dev
```

### Анимации не плавные

Проверьте, что установлен `framer-motion`:
```bash
npm install framer-motion
```

## 📦 Команды

```bash
npm run dev      # Запуск dev-сервера
npm run build    # Сборка для продакшена
npm run preview  # Предпросмотр prod сборки
npm run lint     # Проверка кода
```

## 🎨 Кастомизация дизайна

### Изменить цвета

Файл: `tailwind.config.js`

```javascript
colors: {
  gold: {
    DEFAULT: '#d4af37',  // Ваш цвет
  },
}
```

### Изменить шрифты

Файл: `tailwind.config.js`

```javascript
fontFamily: {
  sans: ['Your Font', 'system-ui'],
}
```

## 📱 Следующие шаги

1. ✅ Запустите проект локально
2. ✅ Проверьте все страницы
3. ✅ Замените моковые данные
4. ✅ Обновите контакты
5. ✅ Задеплойте на Vercel/Netlify
6. ✅ Создайте Mini App в Telegram
7. ✅ Протестируйте в Telegram

## 💡 Полезные ссылки

- [Документация Telegram Mini Apps](https://core.telegram.org/bots/webapps)
- [Tailwind CSS](https://tailwindcss.com/docs)
- [Framer Motion](https://www.framer.com/motion/)
- [React Router](https://reactrouter.com/)

---

Нужна помощь? Откройте `README.md` или `DEPLOY.md`

