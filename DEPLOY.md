# Инструкция по деплою Telegram Mini App

## 🚀 Деплой на Vercel (Рекомендуется)

### Вариант 1: Через GitHub

1. Создайте репозиторий на GitHub и запушьте код
2. Зайдите на [vercel.com](https://vercel.com)
3. Нажмите "Import Project"
4. Выберите ваш репозиторий
5. Настройки:
   - Framework Preset: `Vite`
   - Build Command: `npm run build`
   - Output Directory: `dist`
6. Нажмите "Deploy"

### Вариант 2: Через CLI

```bash
# Установка Vercel CLI
npm install -g vercel

# Логин
vercel login

# Деплой
vercel

# Для продакшен деплоя
vercel --prod
```

После деплоя вы получите URL вида: `https://your-app.vercel.app`

## 🌐 Деплой на Netlify

### Через CLI

```bash
# Установка Netlify CLI
npm install -g netlify-cli

# Логин
netlify login

# Деплой
netlify deploy

# Для продакшен деплоя
netlify deploy --prod
```

### Через веб-интерфейс

1. Зайдите на [netlify.com](https://netlify.com)
2. Drag & Drop папку `dist` после сборки (`npm run build`)

## 🤖 Настройка в Telegram

### 1. Создание бота через BotFather

Откройте [@BotFather](https://t.me/BotFather) и выполните:

```
/newbot
```

Следуйте инструкциям:
- Имя бота: `Недвижимость Сочи Крым`
- Username: `sochi_crimea_realty_bot` (должен заканчиваться на _bot)

### 2. Создание Mini App

В BotFather выполните:

```
/newapp
```

Параметры:
- **Выберите бота**: ваш созданный бот
- **Название**: `Каталог недвижимости`
- **Описание**: `Премиальная недвижимость в Сочи и Крыму. Дома, квартиры, апартаменты от 10 млн рублей.`
- **Фото**: загрузите квадратную картинку 512x512px
- **Демо GIF**: загрузите анимацию или скриншот
- **Web App URL**: ваш URL с Vercel/Netlify

### 3. Настройка меню и кнопок

#### Установка кнопки Menu

```
/setmenubutton
```

Выберите бота и введите:
- Text: `🏠 Открыть каталог`
- URL: ваш URL приложения

#### Добавление описания

```
/setdescription
```

Текст:
```
Каталог премиальной недвижимости на южном побережье России.

🏠 Дома от 10 млн до 2+ млрд рублей
🏢 Квартиры и апартаменты от 0 до 2+ млрд рублей
📍 Сочи и Крым

Найдите свою идеальную недвижимость!
```

#### Установка команд

```
/setcommands
```

Команды:
```
start - Открыть каталог
help - Помощь
contacts - Контакты
```

### 4. Настройка аватара бота

```
/setuserpic
```

Загрузите квадратное изображение для аватара бота.

## 🧪 Тестирование

### Локальное тестирование с ngrok

1. Установите ngrok:
```bash
# Windows (через chocolatey)
choco install ngrok

# macOS (через homebrew)
brew install ngrok

# Или скачайте с https://ngrok.com/download
```

2. Запустите dev-сервер:
```bash
npm run dev
```

3. В новом терминале запустите ngrok:
```bash
ngrok http 5173
```

4. Скопируйте HTTPS URL (например: `https://abc123.ngrok.io`)

5. Обновите URL в BotFather:
```
/editapp
# Выберите бота и приложение
# Web App URL -> вставьте ngrok URL
```

6. Откройте бот в Telegram и протестируйте

### Важно для тестирования

- ✅ Используйте только HTTPS URL
- ✅ Telegram Web App SDK работает только в Telegram
- ✅ Некоторые функции (haptic feedback) работают только на мобильных устройствах

## 🔐 Переменные окружения (если нужны)

Создайте файл `.env`:

```env
VITE_API_URL=https://your-api.com
VITE_TELEGRAM_BOT_TOKEN=your_bot_token
```

Добавьте в Vercel/Netlify через веб-интерфейс:
- Vercel: Settings → Environment Variables
- Netlify: Site settings → Environment variables

## 📊 Аналитика (опционально)

### Настройка Google Analytics

1. Установите:
```bash
npm install @vercel/analytics
```

2. Добавьте в `src/main.jsx`:
```javascript
import { Analytics } from '@vercel/analytics/react';

createRoot(document.getElementById('root')).render(
  <StrictMode>
    <App />
    <Analytics />
  </StrictMode>
);
```

## 🔄 Обновление приложения

После изменений в коде:

```bash
# Коммит изменений
git add .
git commit -m "Update"
git push

# Vercel/Netlify автоматически задеплоят новую версию
```

Или через CLI:

```bash
# Vercel
vercel --prod

# Netlify
netlify deploy --prod
```

## 🐛 Отладка

### Проблема: Белый экран в Telegram

**Решение:**
1. Проверьте консоль браузера в Telegram Desktop
2. Убедитесь, что используете HTTPS
3. Проверьте, что все скрипты загружаются

### Проблема: Haptic feedback не работает

**Решение:**
- Haptic feedback работает только на мобильных устройствах
- Убедитесь, что вибрация включена в настройках Telegram

### Проблема: Неправильные цвета/тема

**Решение:**
- Проверьте загрузку Tailwind CSS
- Убедитесь, что `npm run build` прошел успешно
- Очистите кеш браузера

## 📱 Поддержка устройств

- ✅ iOS 12+
- ✅ Android 5.0+
- ✅ Telegram Desktop
- ✅ Все современные браузеры

## 🎯 Чек-лист перед запуском

- [ ] Код загружен на GitHub
- [ ] Приложение задеплоено на Vercel/Netlify
- [ ] Получен HTTPS URL
- [ ] Бот создан в BotFather
- [ ] Mini App создано и URL установлен
- [ ] Добавлены описание и команды
- [ ] Загружен аватар бота
- [ ] Протестировано на мобильном устройстве
- [ ] Проверены все страницы и переходы
- [ ] Заменены моковые данные на реальные (опционально)

## 💡 Дополнительные возможности

### Добавление платежей

```javascript
// Telegram Payments API
window.Telegram.WebApp.openInvoice(invoice_url);
```

### Отправка данных в бот

```javascript
// Отправка данных обратно в бот
window.Telegram.WebApp.sendData(JSON.stringify(data));
```

### Уведомления

```javascript
// Показать уведомление
window.Telegram.WebApp.showAlert('Заявка отправлена!');
```

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте README.md
2. Изучите документацию Telegram Mini Apps
3. Свяжитесь с разработчиками

---

Удачного деплоя! 🚀

